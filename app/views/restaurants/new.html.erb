<%= simple_form_for(@restaurant) do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.input :category %>
  <%= f.submit "Submit new restaurant", class: "btn btn-primary" %>
<% end %>

<%= link_to "Back to restaurants", restaurants_path %>
